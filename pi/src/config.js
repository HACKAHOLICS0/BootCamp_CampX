// API URLs and other configuration
const config = {
    API_URL: 'http://localhost:5001',  // Updated port to match backend server port
    endpoints: {
        courses: '/api/courses',
        quizzes: '/api/quiz',
        modules: '/api/modules',
        categories: '/api/categories',
        users: '/api/users'
    }
};

export default config;
