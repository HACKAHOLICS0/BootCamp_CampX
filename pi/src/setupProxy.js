// Ce fichier est automatiquement détecté par react-scripts
// Il n'est pas nécessaire d'importer ce fichier dans votre application

// Utiliser la configuration proxy dans package.json à la place
// "proxy": "http://localhost:5000"

module.exports = function(app) {
  // Aucune configuration de proxy ici
  console.log('Proxy configuration is handled by package.json');
};
